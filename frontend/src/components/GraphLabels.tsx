// GraphLabels is no longer used — HTML overlay labels were part of the old
// Three.js right panel. The 2D SVG AgentGraph in the left panel renders its
// own inline labels. This stub is kept to avoid TypeScript project errors.

import React from 'react';

const GraphLabels: React.FC = () => null;
export default GraphLabels;
